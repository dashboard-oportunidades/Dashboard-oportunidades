# Copia este ficheiro para ".env" (sem ".example") e preenche os valores.
# O ".env" fica só no teu PC — nunca é enviado para o GitHub (está no .gitignore).

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
MAIL_TO=
NTFY_TOPIC=
